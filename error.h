#include <iostream>
#include <cstdio>
#include <string>
#include <map>

void error(std::string s);
void expected(std::string s);
void match(int a);
bool matchLen(int length);
bool matchLen(int length, std::string errorString);
